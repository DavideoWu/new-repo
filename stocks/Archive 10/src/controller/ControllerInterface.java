package controller;

/**
 * The interface of the controller of this program.
 * This takes the go method, which goes through all the possible cases the user may encounter.
 */
public interface ControllerInterface {
  void control();
}